{% extends "_layouts/cp" %}

{% block content %}
<div id="sidebar">
    <nav>
        <ul>
            <li>
                <a class="sel" href="{{ url('sp/settings') }}">Settings</a>
                <a class="sel" href="{{ url('sp/import') }}">Import</a>
                <a class="sel" href="{{ url('sp/tools') }}">Tools</a>
                <a class="sel" href="{{ url('sp/spbase') }}">Skills Base</a>
            </li>
        </ul>
    </nav>
</div>
{% endblock %}
