{% requireLogin %}
{% extends "_layout" %}

{% block content %}
    <section class="page dashboard">
        <div class="page-subHeader primary-bg">
            {% include '_includes/topNavigation' %}
        </div>
        <div class="page-content">
        <div id="pending-payment" data-id="{{ entryId }}" data-redirect="{{ redirect }}">
            <h2>Payment Received</h2>
            <h4>Please wait while we confirm payment.</h4>
        </div>
        </div>
    </section>
{% endblock %}