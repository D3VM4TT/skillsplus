{% do craft.lantra.requirePermission('editReports') %}
{% extends "_layout" %}

{% set entryId = craft.app.request.getSegment(4) %}
{% set report = craft.entries.id(entryId).one %}


{% set metaTitle = "Reporting > Data" %}
{% set dateFormat = craft.lantra.setting('themeDateFormat', 'd-m-Y') %}
{% set pageLimit = craft.lantra.setting('themeDefaultLimit', 10) %}

{% set criteria = craft.lantra.reportDataCriteria(report) %}
{% if criteria %}{% paginate criteria as pageInfo, elements %}{% endif %}

{% block content %}
    <div class="page-subHeader primary-bg">
        {% include '_includes/topNavigation' %}
    </div>
    <section class="page">
        <div class="page-header secondary-bg">
            <h2>{{ report.title }}</h2>
            <div class="header-controls">
                <div class="header-search">
                    <a href="/reporting/edit/{{ report.id }}" class="button primary-bg">Edit Report</a>
                </div>
            </div>
        </div>
        {% if elements is defined and elements|length %}
            <div class="page-content">
                <table class="data">
                    <thead>
                    <tr>
                        <th>#</th>
                        <th class="title">Title</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for element in elements %}
                        <tr class="item">
                            <td>{{ element.id }}</td>
                            <td>{{ element.fullName }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            {% include '_includes/pagelinks' with {pageInfo:pageInfo} %}
        {% else %}
            <div class="page-content">
                <h3>This report contains no data.</h3>
            </div>
        {% endif %}
    </section>
{% endblock %}
