{% if craft.request.lastSegment == 'csv' %}{% endif %}
{% set subordinate = craft.users.id(craft.request.segment(3)).first() %}
{% if not subordinate or not craft.lantra.isManager(subordinate.id) %}{% redirect 'reporting/users' %}{% endif %}

{% extends "_layout" %}
{% import "_macros" as ui %}

{% set metaTitle = "Reporting > User > " ~ subordinate.fullName %}
{% set dateFormat = globalsSystem.dateFormat|default('d/m/y') %}
{% set pageLimit = globalsSystem.defaultLimit|default(10) %}

{% block content %}
<section class="list">
    <div class="page-header">
        <h1>User Report {{ subordinate.fullname }}</h1>
    </div>
    {% include '_includes/cpd' with{cpdUser:subordinate} %}
</section>
{% endblock %}
