{% if not currentUser %}
<nav class="topNavigation">
{% for pageEntry in globalsTheme.themeNavigationPublic %}
<a class="" href="{{ pageEntry.getUrl() }}">{{ pageEntry.title }}</a>
{% endfor %}
{% endif %}

{% if currentUser %}
<a class="link-bold" href="/">My Dashboard</a>
<nav class="topNavigation">
{% for pageEntry in globalsTheme.themeNavigationPrivate %}
<a class="" href="{{ pageEntry.getUrl() }}">{{ pageEntry.title }}</a>
{% endfor %}
<a class="link-bold" href="{{ logoutUrl }}">Sign out</a>
</nav>
{% endif %}