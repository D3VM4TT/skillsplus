{% requireLogin %}

{% set account = currentUser %}
{% set accountPhoto = account.getPhoto() %}
{% set accountPhotoUrl = accountPhoto ? accountPhoto.getUrl('thumb') : '/assets/img/img-team-management.jpg' %}

{% set package = craft.lantra.getUserPackage(packageId, account.id) %}
{% if not package %}{% redirect '/profile/taskbooks' %}{% endif %}

{% extends "_layout" %}

{% block content %}
<section class="page dashboard">
    <div class="page-subHeader primary-bg">
        {% include '_includes/topNavigation' %}
    </div>
    <div class="page-header secondary-bg">
        <a href="/">
            <img src="{{ accountPhotoUrl }}" width="150" height="150" class="right rounded-circle" alt="{{ account.fullName }}">
        </a>
        {{ currentUserCompany|default('') }}
        <h2>My taskbooks</h2>
        <p>Manage your taskbook packages.</p>
    </div>
    <div class="page-content">
        <h2>{{ package.packageCoreModule.one.title }} ~ {{ craft.lantra.taskbookLevel(package.packageCoreLevel) }}</h2>
        <p>Created {{ package.packageDateCreated|date('d/m/y') }}</p>
        {% for optionalModuleBlock in package.packageOptionalModules.all %}
            {% set module = optionalModuleBlock.optionalModule.one %}
            <p>{{ module.title }} ({{ craft.lantra.taskbookLevel(optionalModuleBlock.optionalLevel) }})</p>
        {% endfor %}
        <p><strong>£{{ package.packageCost }}</strong>

        {% if not package.packagePaid %}
            <div class="container-btn">
                {{ craft.lantra.payPalButton('Skills+ Taskbook', package.packageCost, 'Pay Now', siteUrl('profile/taskbooks/view/' ~ package.id), {'packageId':package.id})|raw }}
    </div>
        {% else %}
            PAID
        {% endif %}
        </p>
    </div>
</section>
{% endblock %}
