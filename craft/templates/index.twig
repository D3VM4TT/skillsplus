{% set currentUserCompany = craft.lantra.userCompany() %}
{% extends "_layout" %}


{% block content %}
    <section class="page dashboard">
        <div class="page-subHeader primary-bg">
        {% include '_includes/topNavigation' %}
        </div>
        <div class="page-header secondary-bg">
            <a href="/profile"><img src="{{ (currentUser.photoUrl ? currentUser.getPhotoUrl(150) : '/assets/img/img-team-management.jpg') }}" width="150" height="150" class="right rounded-circle" alt="{{ currentUser.fullName }}"></a>
            {#
            {{ currentUserCompany|default('') }}
            #}
            <h2>Hello {{ currentUser.firstName }},</h2>
            <p class="welcome-back">welcome back to your {{ craft.lantra.setting('schemeName') }} dashboard.</p>
        </div>
        <div class="page-content">
            {% if craft.lantra.canManage() %}
                {% include '_includes/scheme' %}
            {% endif %}
            {% include '_includes/cpd' with{cpdUser:currentUser} %}
             {% if craft.lantra.canManage() %}
                {% set pendingEntryCount = craft.lantra.managerCountEndorsementUsers() %}
                {% if not currentUser.managerReadOnly and pendingEntryCount %}
                    <article class="endorsements-pending col-pd-btm secondary-bg">
                    <h4>Endorsements</h4>
                    <div class="status right">
                    <p>You have <a href="/management/endorsements">{{ pendingEntryCount }} result(s)</a> to endorse.</p>
                    </div>
                    </article>
                {% endif %}
            {% endif %}
        </div>
    </section>
{% endblock %}
