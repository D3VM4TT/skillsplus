{% extends "_layout" %}

{% set userId = craft.request.getSegment(3) %}
{% if userId %}
    {% set account = craft.users.id(userId).first %}
    {% if not account or not craft.lantra.isManager(account.id) %}{% exit 403 %}{% endif %}
{% else %}
    {% set account = currentUser %}
{% endif %}

{% set metaTitle = 'Qualifications > ' ~ account.fullname %}

{% block content %}
    <section class="page">
        <div class="page-header">
            <h2>Qualifications for {{ account.fullname }}</h2>
            <p>Create and submit a new qualification.</p>
        </div>
        {%  include 'cpd/_qualifications/table' with {author:account} %}
        {% if not userId or not currentUser.managerReadOnly %}
            <div class="page-content">
                <h3>Add new qualification</h3>
            {%  include 'cpd/_qualifications/form' with {author:(userId ? account : null)} %}
        {% endif %}
            </div>
    </section>
{% endblock %}
