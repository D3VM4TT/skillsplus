{% import "_macros" as ui %}
{% if entry is defined %}
    {{ ui.errorList(entry.getAllErrors()) }}
{% endif %}
<form method="post" role="form" accept-charset="UTF-8" enctype="multipart/form-data">
    {{ getCsrfInput() }}
    <input type="hidden" name="action" value="entries/saveEntry">
    <input type="hidden" name="typeId" value="10">
    <input type="hidden" name="sectionId" value="10">
    <input type="hidden" name="enabled" value="1">
    {% if userId != currentUser.id %}
        <input type="hidden" name="authorId" value="{{ userId }}">
    {% endif %}
    <input type="hidden" name="fields[resultStatus]" value="pending">
    <input type="hidden" name="fields[resultUnit][]" value="{{ entry.id }}">
    <div class="field--wrapper">
        <label for="resultStartDate" class="">Start date (if applicable)</label>
        <input name="userStartDate" type="text" class="-label datepicker-here" placeholder="Select start date" data-language="en" data-position='top left' value="" data-date-format="yyyy-mm-dd">
    </div>
    <div class="field--wrapper">
        <label for="resultFinishDate" class="">Finish date (if applicable)</label>
        <input name="userFinishDate" type="text" class="-label datepicker-here" placeholder="Select finish date" data-language="en" data-position='top left' value="" data-date-format="yyyy-mm-dd">
    </div>
    <div class="field--wrapper">
        <label for="address" class="">Expiry date (if applicable):</label>
        <input name="userExpiryDate" type="text" class="-label datepicker-here" placeholder="Select expiry date" data-language="en" data-position='top left' value="" data-date-format="yyyy-mm-dd">
    </div>
    <div class="field--wrapper">
        <label>Add files (.doc .pdf .csv):</label>
        <input type="file" name="fields[resultEvidence][]" multiple>
    </div>
    <div class="field--wrapper">
        <label>Result notes:</label>
        <textarea name="fields[resultNotes]"></textarea>
    </div>
    <div class="container-btn">
        <button class="button primary-bg" type="submit">Submit evidence</button>
    </div>
</form>
