<div class="header" data-target="#m{{ moduleElementId }}">
    <h4>{{ module.title }}</h4>
    <h6>{{ craft.lantra.taskbookLevel(level) }}</h6>
</div>
{% set resultPackageId = package is defined and package.taskbook.unitResultRollover ? false : package.id %}
{% include 'record/_modules/unitGroups' with {moduleElementId:moduleElementId} %}