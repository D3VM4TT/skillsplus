{% macro errorList(errors) %}
    {% if errors %}
    <div class="notifications">
        {% for error in errors %}
            <div class="alert alert-warning">{{ error }}</div>
        {% endfor %}
    </div>
    {% endif %}
{% endmacro %}
